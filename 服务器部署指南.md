# 服务器部署指南

## ⚠️ 架构变更：服务器本地推流 → AI 电脑推流到服务器

**背景**：服务器配置太低，跑 ffmpeg 编码（libx264）CPU 100%+ 推不动。已改为：
- **编码 + 推流在 AI 电脑**（高配机，NVENC 硬件编码，每台负责一个房间）
- **服务器只装 SRS**（接收 RTMP → 转 HLS，零编码负载，CPU < 10%）+ Nginx 分发

```
AI电脑 ffmpeg(NVENC) ──RTMP──→ 服务器 SRS(:1935) ──转HLS──→ Nginx /hls/ 分发
```

**部署变化**：
| 项目 | 旧（服务器本地推流） | 新（AI 电脑推流） |
|---|---|---|
| 服务器编码进程 | Supervisor 跑 channel-worker + ffmpeg | **已移除**，不再在服务器编码 |
| 服务器新增 | 无 | **SRS**（见 `docs/srs-deploy.md`） |
| AI 电脑 | 无 | 跑 `worker.py`（见 `services/channel-worker/python/README_STREAM.md`） |
| 后台推流按钮 | 开播/关播（启停服务器进程） | **改为只读状态灯**（监控 AI 电脑推流情况） |

**迁移步骤**：
1. 服务器：按 [`docs/srs-deploy.md`](docs/srs-deploy.md) 安装 SRS，并清理旧的 channel-worker Supervisor 程序
2. AI 电脑：按 [`services/channel-worker/python/README_STREAM.md`](services/channel-worker/python/README_STREAM.md) 配置并启动 `start_streams.bat`
3. 本仓库的 PHP/前端代码已适配新架构（RoomService 查 SRS API、后台状态灯、ChannelWorkerManager 改为状态层）

> 下方的「原服务器部署步骤」仍适用于 PHP/Nginx/MySQL/Redis 部分；channel-worker / ffmpeg 相关章节已过时。

---

## 服务器信息

- IP: `38.181.44.164`
- 用户: `root`
- SSH Key: `C:\Users\a1033\.ssh\douyin_38_181`
- 宝塔面板: `https://38.181.44.164:10896/b8555de2` (用户名 `qnl0gnhu`)
- Web 目录: `/www/wwwroot/douyin/`

## 目录结构

```
/www/wwwroot/douyin/
├── vue/dist/          # 主前端 (Vite SPA)
│   ├── index.html     # 入口
│   ├── js/            # JS chunk 文件
│   ├── assets/        # CSS / 图片
│   └── admin/         # BuildAdmin 面板 (独立部署)
├── php/               # PHP 后端 (ThinkPHP)
│   ├── public/        # Nginx root for .php
│   │   └── index.php  # 入口
│   ├── app/
│   │   ├── admin/     # Admin 模块 (BuildAdmin 后端)
│   │   ├── api/       # API 模块 (默认应用)
│   │   └── common/    # 公共模块
│   └── config/
├── hls/               # HLS 视频分片
│   └── room/{id}/
│       ├── index.m3u8
│       └── seg_*.ts
└── services/
    └── channel-worker/ # 推流服务 (Supervisor 管理)
```

## 部署步骤

### 1. 前端部署 (只传变更文件，不要全量!)

> **大坑: 1457 个静态资源文件一起传会非常慢，只传 js/css/index.html 即可。**

```bash
# 本地构建
cd d:\ever\douyin\douyin\vue
npm run build

# 部署 JS + index.html (不传 assets 图片)
python .trae/deploy_frontend_js.py

# 部署 CSS (如果 CSS hash 变了)
python .trae/deploy_css.py
```

部署脚本位置:
- `.trae/deploy_frontend_js.py` — 上传 `dist/index.html` + `dist/js/*.js`
- `.trae/deploy_css.py` — 上传 `dist/assets/*.css`

### 2. PHP 后端部署

```bash
python .trae/deploy_php_restart.py
```

这个脚本会:
1. 上传 `FfmpegCommandBuilder.php` 和 `ChannelWorker.php`
2. **stop** ChannelWorker (Supervisor)
3. **清理旧 HLS 分片** (`rm -f /www/wwwroot/douyin/hls/room/1/*.ts *.m3u8`)
4. **start** ChannelWorker
5. 等待 ffmpeg 产出新分片

> **注意**: 重启会清空 HLS 分片，直播间需要刷新页面。

### 3. Admin 面板部署

```bash
# 本地构建
cd d:\ever\douyin\douyin\php\web
npm run build

# 部署到服务器
python .trae/deploy_all.py
```

Admin 部署到 `/www/wwwroot/douyin/vue/dist/admin/`，避免 Nginx `alias` + `try_files` 的兼容性 bug。

### 4. Nginx 配置更新

需要修改的配置文件: `/www/server/panel/vhost/nginx/38.181.44.164.conf`

关键注意点:

```nginx
# Admin: $uri 必须跟在 /index.php 后面，否则 ThinkPHP 不知道是 admin 应用!
location /admin/ {
    try_files $uri $uri/ /index.php$uri$is_args$args;  # 不是 /index.php$is_args$args
}

# API: 同理
location /api/ {
    try_files $uri /index.php$uri$is_args$args;
}

# HLS: 使用 alias 而不是 root
location /hls/ {
    alias /www/wwwroot/douyin/hls/;
    add_header Access-Control-Allow-Origin *;
    add_header Cache-Control 'no-cache, no-store, must-revalidate';
}
```

部署 Nginx 后必须 **reload**:
```bash
nginx -t && nginx -s reload
```

### 5. ChannelWorker 手动控制

```bash
# 停止
supervisorctl stop channel-worker-room1

# 启动
supervisorctl start channel-worker-room1

# 查看状态
supervisorctl status

# 确认无孤儿 ffmpeg
ps aux | grep ffmpeg | grep -v grep
```

## 常见坑

### 坑1: 前端部署不要全量上传
- **现象**: 1457 个文件 SFTP 上传极慢
- **解决**: `deploy_frontend_js.py` 只传 index.html + js/*.js (约30个文件)
- CSS 变了才单独传 CSS

### 坑2: Nginx alias + try_files $uri 不兼容
- **现象**: 静态文件 404
- **原因**: `alias` 下的 `try_files $uri` 有已知 bug
- **解决**: 把 admin dist 放到 `vue/dist/admin/`，直接用 root 路径

### 坑3: Nginx try_files 丢失原始 URI
- **现象**: Admin POST 请求返回错误数据 (api 应用的响应而非 admin 应用的)
- **原因**: `try_files ... /index.php$is_args$args` 没传 `$uri`
- **解决**: 改成 `/index.php$uri$is_args$args`

### 坑4: ChannelWorker stop 后 ffmpeg 孤儿进程
- **现象**: Supervisor stop 后 ffmpeg 还在跑，CPU 100%+
- **原因**: PHP 收到 SIGTERM 后退出，但 proc_open 的子进程变成孤儿
- **解决**: ChannelWorker.php 中加入 `pcntl_signal(SIGTERM, ...)` 处理器，在 `runFfmpeg()` 中定时调用 `pcntl_signal_dispatch()`
- **验证**: stop 后 `ps aux | grep ffmpeg` 应无输出

### 坑5: HLS 码率过高导致播放不了
- **现象**: 分片 6-11MB，浏览器下载超时，FRAG_BUFFERED 不触发
- **解决**: ffmpeg 用 `libx264` 重编码(非 `-c copy`)，720p@30fps, CRF 28, maxrate 2500k
- **结果**: 分片 ~650KB，秒级加载

### 坑6: 视频画面只有中间一小块
- **现象**: 16:9 视频在竖屏上只有中间一条
- **原因**: CSS `object-fit: contain` 保持比例但留黑边
- **解决**: 改成 `object-fit: cover` (填满屏幕、裁切两侧)

### 坑7: 数据库连接失败
- **现象**: ChannelWorker 报 MySQL 连接错误
- **原因**: Supervisor 的 `environment=` 配置不生效，进程仍用默认值
- **解决**: 直接在 `Database.php` 中 hardcode 服务器凭据:
  ```php
  DB_NAME: 'zhibo'      (不是 'live_platform')
  DB_USER: 'zhibo'      (不是 'root')
  DB_PASSWORD: '12345678' (不是 'root')
  ```

### 坑8: 部署后页面还是旧版
- **解决**: 用户必须 **Ctrl+Shift+R 强制刷新** (清除浏览器缓存)
- JS/CSS 文件名带 hash，但 index.html 可能被缓存

## 调试命令速查

```bash
# 查看 CPU/进程
ps aux --sort=-%cpu | head -10
uptime

# 查看 HLS 分片
ls -lhS /www/wwwroot/douyin/hls/room/1/seg_*.ts | tail -5
cat /www/wwwroot/douyin/hls/room/1/index.m3u8

# 测试 Nginx 路由
curl -sI -H "Host: 38.181.44.164" http://127.0.0.1/admin/
curl -s -X POST -H "Host: 38.181.44.164" -d "username=admin&password=xxx" http://127.0.0.1/admin/Index/login

# 查看 Nginx 日志
tail -20 /www/wwwlogs/38.181.44.164.error.log

# Supervisor
supervisorctl status
supervisorctl stop channel-worker-room1
supervisorctl start channel-worker-room1
```

## 部署脚本清单

| 脚本 | 用途 |
|------|------|
| `.trae/deploy_frontend_js.py` | 部署主前端 JS + index.html |
| `.trae/deploy_css.py` | 部署主前端 CSS |
| `.trae/deploy_php_restart.py` | 部署 PHP + 重启 ChannelWorker |
| `.trae/deploy_all.py` | 部署 Admin 面板 + Nginx 配置 |
| `.trae/check_cpu.py` | 查看服务器 CPU 状态 |
| `.trae/check_admin.py` | 诊断 Admin 路由问题 |
| `.trae/kill_old_ffmpeg.py` | 清理孤儿 ffmpeg 进程 |
